#include ./corelib.ahk

; Select color
  ;; return RGB color at cursor

; Get Coordinates
  ;; return coordinates at cursor

; Search Loop
  ;; stopSignal := false
  ;; search for array of colors in given area
  ;; stopSignal is set to false whenever loop is initiated
  ;; break out of loop when stopSignal == true
  ;; activate given action when color found